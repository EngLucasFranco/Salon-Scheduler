{
  "name": "agenda-salao",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:apps": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently --kill-others-on-fail --names API,WEB --prefix-colors blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start:api": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20 <25"
  }
}
